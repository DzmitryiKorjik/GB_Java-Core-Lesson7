package Lesson7.project;

import java.io.IOException;
import java.time.Period;

public interface WeatherModel {
    default void getWeather(String selectedCity, Period period) throws IOException {

    }

    void getWeather(String selectedCity, Lesson7.project.Period now);
}
